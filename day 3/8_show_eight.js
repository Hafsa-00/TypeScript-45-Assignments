"use strict";
// Program by Hafsa, 16/02/2024
// This program performs basic arithmetic operations and prints the results.
let add = (8 + 0); // Addition operation
let minus = (8 - 0); // Subtraction operation
let mutiply = (8 * 1); // Multiplication operation
let divide = (8 / 1); // Division operation
console.log(`add: ${add},
minus:${minus},
mutiply:${mutiply},
divide: ${divide}`);
